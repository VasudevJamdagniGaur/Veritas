// Main Veritas API (analyze, trust for registered users).
window.VERITAS_API_BASE = "http://localhost:5000/api";

// Instagram account scores use the extension background → FastAPI XGBoost.
// Start from veritas-backend: uvicorn model:app --reload (default http://127.0.0.1:8000).
// URLs for that live in background.js (service worker has no window).

